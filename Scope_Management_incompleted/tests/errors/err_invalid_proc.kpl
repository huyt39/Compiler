PROGRAM INVALIDPROC;
CONST 
  MAX = 100;
VAR 
  X : INTEGER;

BEGIN
  X := 10;
  
  (* lỗi: X là một biến, không thể được gọi như một thủ tục *)
  CALL X; 

  (* lỗi: MAX là một hằng số, không thể được gọi như một thủ tục *)
  CALL MAX; 
END.